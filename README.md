# Simple Nvim Configuration - Minimum Version

A simple neovim configuration that does as many things naively within neovim as possible.

Plugins that are used are related to syntax highlighting and LSP configuration. The after directory is used to apply any keybindings related to plugins

This version of the configuration is striped down and doesn't use many plugins

### Getting started

Simple clone this repository where your neovim configuration would live
Either in your home directory under .nvim or in .config/nvim

Then open neovim and Lazy will pull all necessary plugins and this configuration works out of the box

### Usage

Here is a quick reference to the keybindings made for Neovim

